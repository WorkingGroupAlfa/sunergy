'use client';
import { useState } from 'react';import { motion } from 'framer-motion';import { ChevronDown } from 'lucide-react';import { cn } from '@/lib/utils';
export function Faq({items}:{items:Array<{q:string;a:string}>}){const [active,setActive]=useState(0);return <div className="space-y-4">{items.map((item,index)=>{const isActive=index===active;return <motion.div key={item.q} className="panel-card overflow-hidden" initial={{opacity:0,y:12}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.25}} transition={{duration:.35,delay:index*.04}}><button type="button" className="flex w-full items-center justify-between gap-4 px-6 py-5 text-left" onClick={()=>setActive(isActive?-1:index)}><span className="text-lg font-medium text-ink">{item.q}</span><ChevronDown className={cn('h-5 w-5 text-steel transition',isActive&&'rotate-180')}/></button><div className={cn('grid transition-all duration-300',isActive?'grid-rows-[1fr]':'grid-rows-[0fr]')}><div className="overflow-hidden"><p className="px-6 pb-5 text-sm leading-7 text-steel">{item.a}</p></div></div></motion.div>})}</div>}
